# Requiere Python 3.11 o 3.12. mlx-whisper depende de numba, que suele tardar meses
# en publicar wheels para versiones de Python recién salidas (3.13/3.14 aún no
# funcionan bien al escribir esto) — usa un Python algo más antiguo para este venv.

# Núcleo del pipeline (multiplataforma)
numpy>=1.26

# Transcripción (PASO 1) — requiere Apple Silicon.
mlx-whisper>=0.4

# Detección de rostro para centrar los punch-ins (PASO 5). Opcional: si no está
# instalado (o falla), el pipeline usa un encuadre por defecto y avisa.
# Fijado a la serie 4.x: la 5.0 (recién salida) rompe cv2.CascadeClassifier.
opencv-python-headless>=4.9,<5

# Música (PASO 10) — requiere Apple Silicon. NO está en PyPI: no lo instales con pip
# desde este archivo. Instálalo aparte, con el venv de este proyecto activado:
#   git clone https://github.com/andrade0/musicgen-mlx.git ~/musicgen-mlx-src
#   cd ~/musicgen-mlx-src && make install
# Expone el paquete `audiocraft_mlx` (y el CLI `musicgen-mlx` si el Makefile registra
# el entry point) — ver pipeline/musicgen_backend.py para el detalle exacto de la API.
