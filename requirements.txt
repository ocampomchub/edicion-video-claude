# Núcleo del pipeline (multiplataforma)
numpy>=1.26

# Transcripción (PASO 1) — requiere Apple Silicon.
mlx-whisper>=0.4

# Música (PASO 10) — requiere Apple Silicon. El nombre exacto del paquete varía según
# el fork/versión de musicgen-mlx que instales; si `pip install musicgen-mlx` no
# resuelve, instala desde el repo que estés usando y revisa
# pipeline/musicgen_backend.py (la función _try_python_api documenta qué API espera).
musicgen-mlx>=0.1 ; sys_platform == "darwin"

# Detección de rostro para centrar los punch-ins (PASO 5). Opcional: si no está
# instalado, el pipeline usa un encuadre por defecto (centro-superior) y avisa.
opencv-python-headless>=4.9
